# Topología same-origin (Sprint 9): el backend sirve el SPA, la API es relativa al origen
VITE_API_URL=/api/v1

# S44 — Sólo lo lee el build nativo (ver src/lib/http.ts). La web ignora esta variable.
VITE_NATIVE_API_ORIGIN=https://maat.com.ar
