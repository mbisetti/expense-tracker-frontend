VITE_API_URL=http://localhost:8080/api/v1

# S44 — Origen absoluto de la API para el build NATIVO (Capacitor). En la web no se usa: ahí
# VITE_API_URL es relativa y la sirve el mismo backend (same-origin desde S9). Adentro del
# WebView el origen es capacitor://localhost, así que una ruta relativa no llega a ningún lado.
# Es provisional como el dominio: cambia cuando caiga el NOMBRE.
VITE_NATIVE_API_ORIGIN=https://expense-tracker-marko.up.railway.app
